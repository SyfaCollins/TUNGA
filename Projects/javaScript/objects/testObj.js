// Testing Objects for properties

//Here we get to use the .hasOwnProperty(propname) and this returns true or false

const myObj = {
    'top':'Hat',
    'bottom':'Pants'
}

console.log(myObj.hasOwnProperty('top'))
console.log(myObj.hasOwnProperty('middle'))
